

<%= form_for tag, remote: true do |form| %>
    <%#= form.rich_text_area :content, style: "width: 100%; height: 400px;", class: "shadow p-2 rounded" %>
    <%= form.text_area :content, style: "width: 100%; height: 400px;", class: "shadow p-2 rounded" %>
                <br/>
    <%= form.submit "Save Note",class: "btn btn-outline-primary save-note" %> 
<% end %>
