
    <br>
    <br>

    <div class="container">
        <div class="row">
          <div class="col-12">
            <%= link_to "New Note", new_tag_path, class: "btn btn-outline-primary", data: { "bs-target": "#myModal", "bs-toggle": "modal" }, remote: true %>
            <br/>
            <br/>
          </div>
    <!----------------------title------------------>  
           
          <div class="col-4">
            <div id="CrateNote">
                <% @tags.each do |tag| %>
                  
                    <%= render "tag", tag: tag %>
                    
                <% end %>
            </div>
          </div>
    <!--------------------Notes------------------>    
         
          <div class="col-8">
            <div id="tagRow_<%= tag.id %>">
              <div id="addtag-section">
               <% if Tag.first.present? %>
                 <%= render "edit_form", tag: Tag.first %>
               <% end %>  
              </div>
            </div>
          </div>
        </div>
    </div>


    <!-- The Modal -->
    <div class="modal" id="myModal">
      <div class="modal-dialog modal-lg">
        <div class="modal-content">
          <!-- Modal Header -->
          <div class="modal-header">
            <h4 class="modal-title" data-bs-toggle="modal" data-bs-target="#myModal" >
              <div id="NewTagForm" >
                  
              </div>
            </h4>
          </div>
        </div>
      </div>
    </div>





  