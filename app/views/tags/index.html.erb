
    <br>
    <br>

    <div class="container">
        <div class="row">
          <div class="col-12">
            <%= link_to "New Note", new_tag_path, class: "btn btn-outline-primary", data: { "bs-target": "#myModal", "bs-toggle": "modal" }, remote: true %>
            <br/>
            <br/>
          </div>
    <!----------------------title------------------>  
           
          <div class="col-4">
            <div id="CrateNote">
              <div id="myDIV">
                
                <% @tags.each_with_index do |tag, index| %>
                  
                    <%= render "tag", tag: tag, index: index %>
                  
                <% end %>
              </div>  
            </div>
          </div>
    <!--------------------Notes------------------>    
         
          <div class="col-8">
            <div id="tagRow_<%= tag.id %>">
              <div id="addtag-section">
               <% if Tag.first.present? %>
                 <%= render "edit_form", tag: Tag.first %>
               <% end %>  
              </div>
            </div>
          </div>
        </div>
    </div>


    <!-- The Modal -->
    <div class="modal" id="myModal">
      <div class="modal-dialog" style="width: 400px;">
        <div class="modal-content">
          <!-- Modal Header -->
          <div class="modal-header">
            <h4 class="modal-title" data-bs-toggle="modal" data-bs-target="#myModal" >
              <div id="NewTagForm" >
                  
              </div>
            </h4>
          </div>
        </div>
      </div>
    </div>

    <div class="modal fade" id="editTagModal" tabindex="-1" aria-labelledby="editTagModalLabel" aria-hidden="true">
      <div class="modal-dialog">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="editTagModalLabel">Edit Tag Title</h5>
            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
          </div>
            <div id="EditTagForm">
            
          </div>
        
      </div>
    </div>


<!-- The Modal -->
    <div class="modal" id="editTagModal">
      <div class="modal-dialog" style="width: 400px;">
        <div class="modal-content">
          <!-- Modal Header -->
          <div class="modal-header">
            <h4 class="modal-title" data-bs-toggle="modal" data-bs-target="#editTagModal" >
              <div id="EditTagForm" >
                  
              </div>
            </h4>
          </div>
        </div>
      </div>
    </div>
  












  